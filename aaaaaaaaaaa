| stats
    count() as Requests,
    avg(ProcessingTimeSeconds) as AvgProcSeconds,
    pct(ProcessingTimeSeconds, 95) as P95ProcSeconds,
    max(ProcessingTimeSeconds) as MaxProcSeconds
  by bin(EndTime, 5m)
| sort bin(EndTime, 5m) asc
